import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-web3-champions',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
